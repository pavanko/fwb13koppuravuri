extends layout

block content
  h1= title
  p Rows = #{query.rows} and Columns = #{query.cols}
    -
     rows= Number(query.rows)
      col= Number(query.cols)
    if !Number.isInteger(rows)
        p below mentioned rows are not Integer 
    else if !Number.isInteger(col)
        p below mentioned columns are not Integer 
    else if rows < 0 || rows > 6
        p below mentioned rows are in one to four range 
    else if col < 0 || col > 6
        p below mentioned colums are in one to four range
    else
       table           
            - for(let i=0; i<rows; i++) {
               tr
                 - for(let j=i; j<col+i; j++) {
                     td= j 
                 -}              
            - }
